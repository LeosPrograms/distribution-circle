// Google OAuth disabled for static deployment
// All backend functionality has been removed

export function renderGoogleButton() {
	// Disabled for static deployment
	console.log('Google OAuth disabled in static build')
}

export function initializeGoogleAccounts() {
	// Disabled for static deployment
	console.log('Google OAuth disabled in static build')
}
